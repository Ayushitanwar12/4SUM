# 4SUM
Key Features of the Solution:
Time Complexity: O(n^3) where n is the length of the input array. The time complexity arises due to sorting the array and using two pointers to check for all combinations of quadruplets.

Space Complexity: O(n) due to the space used by the result list and for sorting the array.
